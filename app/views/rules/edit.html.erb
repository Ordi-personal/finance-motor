<%= link_to t("rules.titles.back"), rules_path %>

<%= render DS::Dialog.new do |dialog| %>
  <%
    title = if @rule.name.present?
              t("rules.titles.edit_named", resource_type: @rule.resource_type, name: @rule.name)
            else
              t("rules.titles.edit_generic", resource_type: @rule.resource_type)
            end
  %>
  <% dialog.with_header(title: title) %>

  <% dialog.with_body do %>
    <%= render "rules/form", rule: @rule %>
  <% end %>
<% end %>
